#import "tables.typ": *
#pagebreak()
== 问题二的模型建立与求解

问题二沿用问题一的物理模型，但实际净负荷未知，且库存需要跨日连续传递。本文以“组合预测—分位数修正—日前LP—真实执行”组织调度过程。

=== 负载与光伏预测

根据周节律与天气短期持续性，分别选择前7、14日负载及前1、2、3日光伏作为低维候选：
$ hat(L)_(d,t)=beta^L_(m,1)L_(d-7,t)+beta^L_(m,2)L_(d-14,t), quad bold(beta)^L_m in Delta_2, $
$ hat(R)_(d,t)=sum_(k=1)^3 beta^R_(m,k)R_(d-k,t), quad bold(beta)^R_m in Delta_3. $ <eq-forecast>
其中$m$为月份，$Delta_K={bold(beta) in RR^K: beta_k>=0, sum_k beta_k=1}$。凸组合保持历史曲线的可解释性，避免小样本任意外推；不预设近日期必然具有更大权重。

每月首日向前回看56日，利用已经完成的标签估计权重，并在月内冻结。负载与光伏统一采用带上一期收缩中心的Huber估计@huber：
$ bold(beta)^z_m=arg min_(bold(beta) in Delta_K)
  [frac(1,n_m) sum_(i in cal(W)_m) rho_1.345((y_i-X_i bold(beta))/s_m)
  +lambda_z norm(bold(beta)-bold(beta)^z_(m-1))_2^2]. $ <eq-huber>
稳健尺度$s_m$由训练残差的绝对中位差确定，并设置数值下限。Huber损失为
$ rho_delta(u)=cases(u^2/2 & abs(u)<=delta, delta abs(u)-delta^2/2 & abs(u)>delta). $
小误差按平方惩罚，大误差按线性增长，减少异常天气影响；收缩项限制月间权重跳变。初始中心分别为$(1/2,1/2)$与$(1/3,1/3,1/3)$，SLSQP在单纯形约束下求解。

正则化候选为0、0.003、0.01、0.03、0.1、0.3、1、3。负载自1月15日、光伏自1月4日起进行前向验证，首个无训练标签的可预测日使用均匀先验，其误差仍计入验证；最终均选择$lambda=3$。历史残差采用当时已完成验证所得的参数，不将一月末选出的系数回填到较早日期。

0时可使用上一行前143个已完成标签，但不能使用尚未完成的末格；滞后1日曲线的末格以最近已完成同槽值替代。二月初负载、光伏候选可拟合标签分别为2447和4031个，光伏进一步保留1960个具有出力信息的标签；这与直接剔除整条上一日记录不同。

=== 预测误差的分位数风险修正

记$hat(N)_(d,t)=hat(L)_(d,t)-hat(R)_(d,t)$，前向残差为$r_(j,t)=N_(j,t)-hat(N)_(j,t)$。正残差表示低估净需求。暂不考虑储能耦合，单时段覆盖量$u$的期望费用为
$ J_t(u)=p_t u+5p_t E[(X_t-u)^+]. $ <eq-newsvendor>
在连续分布下，增购一单位的成本$p_t$等于避免短缺的期望收益：
$ J'_t(u^*)=p_t-5p_t Pr(X_t>u^*)=0
  arrow.r F_(X_t)(u^*)=1-1/5=0.8. $ <eq-alpha08>
因此取最近28日可用前向残差的80%分位数，构造
$ tilde(N)_(d,t)=hat(N)_(d,t)+Q_0.8(r_(j,t): (j,t)"已完成且可用"). $ <eq-risk>
逐时隙分位数允许利用上一行前143格；完整多时段场景则必须等待整条路径结束。风险净负荷不截为非负，负值仍可表示光伏富余。80%由题面价格比导出；加入储能跨时隙耦合后，它是具有经济依据的风险基准，而非整个随机系统的解析最优分位数。

=== 储能状态的跨日连续传递

二月至十二月不施加实际库存的每日闭合条件。题目只给出1月1日0时6000 kWh，故按既定工程约定，利用1月1日00:10至2月1日00:00的4463个已完成区间作完全信息初始化：
$ min [sum_(t in cal(J)) p_t g_t-v_S S_"Jan,end"], quad
  v_S=0.4248/0.9=0.472. $ <eq-init>
得到2月1日0时库存7950 kWh。该初始化使用一月已知完整信息，不称为“一月因果运行”；二月至十二月决策仍遵守当前信息边界。三个后续问题共用此初值与继承合同。

每日0时先用已知净负荷估计投影未完成的00:00—00:10边界库存，再形成新计划；随后才以真值执行继承合同。首日实际00:10库存为7975.914 kWh。日内真实库存逐时递推，交易日之间满足
$ S^"act"_(d+1,0)=S^"act"_(d,T). $ <eq-crossday>
此处首末下标表示交易窗口边界，不等同于自然日0时、24时，后者按@eq-natural 单独汇总。

=== 修正后的模型

将@eq-physical 的已知净负荷替换为$tilde(N)_(d,t)$，目标和物理边界保持原LP形式：
$ min sum_(t=1)^T p_t g_(d,t), quad
  "s.t." cases(
  g_(d,t)+d_(d,t)-c_(d,t)-w_(d,t)=tilde(N)_(d,t),
  S_(d,t)=S_(d,t-1)+0.9c_(d,t)-d_(d,t)/0.9,
  1200<=S_(d,t)<=10800,
  0<=c_(d,t)<=5000/6 quad 0<=d_(d,t)<=5000/6,
  g_(d,t)>=0 quad w_(d,t)>=0,
  S_(d,0)=hat(S)^"start"_d quad S_(d,T)=6000).
$ <q2-plan-model>
6000 kWh是*规划参考期末库存*，防止有限时域放空；它不是实际日末硬约束，其取值在灵敏度分析中检验。计划$g$冻结后，以当前真值执行补偿。令缺口$D_(d,t)=N_(d,t)-g_(d,t)$，有
$ d^"act"_(d,t)=min(D_(d,t)^+,macron(q),0.9(S^"act"_(d,t-1)-1200)), $
$ c^"act"_(d,t)=min((-D_(d,t))^+,macron(q),(10800-S^"act"_(d,t-1))/0.9), $
$ e_(d,t)=(D_(d,t)-d^"act"_(d,t))^+, quad
  w^"act"_(d,t)=(-D_(d,t)-c^"act"_(d,t))^+. $ <eq-action>
该规则不同时充放电，也不以紧急购电主动充电。真实状态按@eq-physical 更新，研究期费用为
$ C_2=sum_(d in cal(D))sum_(t=1)^T p_t[g_(d,t)+5e_(d,t)]. $ <eq-q2cash>

=== 模型求解

按日期依次执行：每月更新预测权重；每日生成风险净负荷并求解HiGHS日前LP；锁定合同后逐时执行@eq-action；完成观测后更新残差及真实库存。评价范围为2月1日00:10至次年1月1日00:10，共334天、48096个区间。首个继承午夜区间影响起始库存，但费用属于一月账本；最后一个交易末格属于评价期。

=== 结果分析

正式方案的计划费13331488.00元、紧急费627158.46元，总费13958646.46元。滚动点预测对照为15337036.77元，节费1378390.31元、8.9873%；紧急购电由504223.73降至105404.26 kWh，下降79.0957%，发生703个紧急时隙。增加计划备用量降低了5倍电价短缺损失，说明预测误差必须按经济后果处理。

#figure(image("figures/fig_q2_scheme_bar.pdf", width: 83%), caption: [问题二组合预测与风险修正的费用对照]) <fig-q2-schemes>

以下各问指定日购电表列示*最终有效合同电量（不含紧急购电）*，全天购电费包括紧急费及适用的调整费；表3另列紧急量，合同量与紧急量之和为实际总购电量。该口径避免把0时计划、最终合同与紧急补购混为一谈；0时计划全量保存在工作簿中。充放电表均采用自然日口径。

#result-days("q2", "问题二")
#emergency("q2", "问题二")

指定日期中，3月20日、6月21日和12月21日无紧急购电；9月23日的连续缺口列于上表。“-”表示无对应事件，非缺失数据。所有表格由最终未舍入数组汇总，展示舍入不参与费用复算。更完整的典型日轨迹、方案比较和紧急事件分布见附录。
