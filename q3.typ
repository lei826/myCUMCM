#import "tables.typ": *
#pagebreak()
== 问题三的模型建立与求解

问题三保留前问的稳健预测、风险修正和线性储能约束，将一次日前决策扩展为随新预报到达的剩余时域滚动优化。其增量是信息更新、库存反馈及双向合同调整，而不是更换物理模型。

=== 决策时点与信息时序

设$cal(H)=(0,6,12,18)$，对应已经执行的时隙数$tau_h=(0,35,71,107)$。时点$h$只允许调整$cal(T)_(d,h)={t:t>tau_h}$中的合同，已执行部分满足
$ g^(h)_(d,t)=g^(h^-)_(d,t), quad t<=tau_h. $ <eq-freeze>
其中$g^0$为0时原计划，$g^(h^-)$为前次生效版本。6、12、18时直接读取真实库存，0时先对继承边界作非前视投影：
$ S^(h)_(d,tau_h)=cases(
  Phi(S^"00"_d,g^b_d,hat(N)^b_d) & h=0,
  S^"act"_(d,tau_h) & h in (6,12,18)). $ <eq-statefeedback>
$Phi$为@eq-action 对应的状态转移；边界净负荷取最近已完成区间估计。2月1日0时统一初始化7950 kWh，实际00:10为7975.914 kWh。预报更新与午夜边界均不复位库存。

=== 分时可靠性组合光伏预测

负载继续采用@eq-forecast。先由前三日历史曲线得到$hat(R)^"hist"$，再与截至$h$已发布的整点预报插值曲线$f^(0),...,f^(h)$组合：
$ X^(h)_(d,t)=(hat(R)^"hist"_(d,t),f^(0)_(d,t),...,f^(h)_(d,t)), quad
  hat(R)^(h)_(d,t)=X^(h)_(d,t)bold(omega)^(h)_m. $ <eq-q3forecast>
这与将前三日和全部发布版本直接放在同一个单层模型中不同：最终代码先拟合历史光伏组合，再拟合历史组合与发布预报的可靠性权重。$bold(omega)^(h)_m in Delta_(K_h)$，其中$K_h=2,3,4,5$，采用@eq-huber 的Huber损失及上一期收缩形式估计。

每个决策时点均用最近已完成区间的真实光伏观测连接下一整点预报，并在消融组中保留同一观测锚点。由此，方案差异仅来自允许使用的新增发布版本，而不是偷偷给某组更多当前观测。月度权重拟合保留全部已完成标签；光伏模型可从1月4日开始，完整净负荷候选比较从1月15日开始。正则化候选0.3、1、3经一月前向验证最终选择3，一月历史预测使用各自当时的前缀选参结果。

=== 信息条件匹配的残差风险修正

为使风险估计与实际信息条件一致，定义
$ epsilon^(h)_(j,t)=N_(j,t)-[hat(L)_(j,t)-hat(R)^(h)_(j,t)], quad
  N^(ast,h)_(d,t)=hat(L)_(d,t)-hat(R)^(h)_(d,t)+Q_(alpha_h)(epsilon^(h)_(j,t)). $ <eq-q3risk>
残差库按当前发布时点、所用版本与共同观测锚点匹配，窗口为28日。与第二问逐时隙利用已完成标签不同，本问正式代码统一使用完整历史日路径：0时最多取至前两日，日内可取至前一日；权重拟合仍保留上一行前143个已完成标签，不将拟合窗口与风险路径库混为一谈。

0时仍取$alpha_0=0.8$。日内增购单位成本为$1.5p_t$；取消原合同先退款再支付$0.5p_t$违约金，净节约仅$0.5p_t$。由单时段边际平衡得
$ alpha_"up"=1-frac(1.5p_t,5p_t)=0.7, quad
  alpha_"down"=1-frac(0.5p_t,5p_t)=0.9. $ <eq-band>
因此在无储能耦合的连续分布模型中，原覆盖量位于70%—90%分位区间时可保持不变。该区间为调整规则提供经济依据，不是完整多阶段系统的最优性证明。最终代码在三种训练候选中选定固定0.7分位修正作为正式日内规则。

=== 双向合同调整费用

定义相对0时原计划的增购量$delta^+=(g-g^0)^+$和取消量$delta^-=(g^0-g)^+$。单时隙现金费用为
$ p_t g^0-p_t delta^-+0.5p_t delta^-+1.5p_t delta^++5p_t e
  =p_t[g+0.5 abs(g-g^0)+5e]. $ <eq-q3cash>
例如原合同100 kWh、最终80 kWh时，正常与调整合计按90 kWh的电价支付；最终120 kWh时按130 kWh支付。6、12、18时的中间版本不重复收费，每格只按最终合同对原计划结算一次。

=== 剩余时域滚动线性规划

0时模型沿用@q2-plan-model。日内引入费用变量$z^(h)_(d,t)$，利用两条直线的上包络精确线性化合同费用：
$ z^(h)_(d,t)>=0.5p_t g^(h)_(d,t)+0.5p_t g^0_(d,t), $
$ z^(h)_(d,t)>=1.5p_t g^(h)_(d,t)-0.5p_t g^0_(d,t). $ <eq-envelope>
将其与物理约束合并，剩余时域模型为
$ min sum_(t in cal(T)_(d,h))z^(h)_(d,t), quad "s.t." cases(
  g^(h)_(d,t)+d^(h)_(d,t)-c^(h)_(d,t)-w^(h)_(d,t)=N^(ast,h)_(d,t),
  S^(h)_(d,t)=S^(h)_(d,t-1)+0.9c^(h)_(d,t)-d^(h)_(d,t)/0.9,
  1200<=S^(h)_(d,t)<=10800,
  0<=c^(h)_(d,t)<=5000/6 quad 0<=d^(h)_(d,t)<=5000/6,
  g^(h)_(d,t)>=0 quad w^(h)_(d,t)>=0,
  S^(h)_(d,T)=6000).
$ <q3-intraday-model>
起始库存由@eq-statefeedback 给出，并同时施加@eq-envelope。因为预测内的合同单位边际价格不超过$1.5p_t<5p_t$，参考规划不主动设置紧急购电变量；真实缺口在实际执行后计费。参考轨迹用于生成合同，不机械指定每一次真实充放电，实际仍按@eq-action 执行。这属于带状态反馈的滚动规划@mpc，不是完全信息全年优化。

=== 模型求解

先以一月完整可用前缀进行正则化验证，再比较固定0.7分位、0.7—0.9方向性包络及场景筛选规则，评价覆盖1月15日至31日24:00。不同规则使用同源预测、初始库存和执行器；以库存修正费用$C^"adj"=C+v(S_"start"-S_"end")$作比较，按相邻日块的一标准误准则优先选较简单规则。

正式运行依次执行0时投影与计划、6/12/18时新预测与真实库存反馈、冻结已执行后缀、逐时供需补偿和跨日更新。每个含$n$格的日内LP含$6n$个连续变量，稀疏矩阵由HiGHS求解。单次原始—对偶检验证明参考LP的数值最优性，不证明含未来信息到达的完整策略全局最优。

=== 结果分析

正式方案原计划费13284460.04元，取消退款560465.28元、违约金280232.64元、增购费135248.13元、紧急费360539.49元，合计13500015.02元。实际购电21002912.90 kWh，其中紧急量64038.16 kWh。同信息日前冻结方案为13836019.48元，滚动节费336004.47元，下降2.4285%。

#result-days("q3", "问题三")
#emergency("q3", "问题三")

#figure(image("figures/fig_q3_plan_vs_adjust.pdf", width: 88%), caption: [问题三0时原计划与最终有效合同]) <fig-q3-adjust>

为回答是否需要其他时刻预报，设置8种刷新掩码，所有组共享真实状态反馈和当前观测锚点。按全部加入顺序平均的库存修正节费贡献为
$ phi_h=sum_(A subset.eq cal(H)' without {h}) frac(abs(A)!(3-abs(A)-1)!,3!)
  [V(A union {h})-V(A)], quad cal(H)'={6,12,18}. $ <eq-shapley>
其中$V(A)$为相对于同一基线的节费，采用Shapley分摊@shapley。6时、12时、18时分别贡献34657.64元、40647.45元和-52.33元；这不是把“仅加入一次”的顺序增量直接当贡献。仅增加新预报的合计现金节费为75252.76元，另有260751.71元来自状态反馈及调整机会。

因此6时和12时新预报具有主要价值；在当前数据、共同锚点及参考库存下，18时没有明确额外节费，但18时库存反馈与合同调整机会仍可保留。信息价值图与全部消融组见附录，不能将结论推广为一般情况下18时预报无用。
